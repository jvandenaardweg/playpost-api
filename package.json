{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test --stream",
    "build": "lerna run build --stream",
    "dev": "lerna run dev --stream",
    "publish": "lerna version --force-publish --amend"
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.7.5"
  }
}
