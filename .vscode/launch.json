{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Typescript Server: index",
      // "processId": "${command:PickProcess}",
      "protocol": "inspector",
      "port": 9230,
      "restart": true,
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "."
    },
    {
      "type": "node",
      "request": "attach",
      "name": "Typescript Server: worker",
      // "processId": "${command:PickProcess}",
      "protocol": "inspector",
      "port": 9231,
      "restart": true,
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "."
    }
  ]
}
