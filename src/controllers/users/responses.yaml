components:
  responses:
    PostUsersResponse:
      description: A post Users response
      content:
        'application/json':
          schema:
            type: object
            $ref: '#/components/schemas/User'
