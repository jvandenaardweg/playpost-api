components:
  schemas:
    StripeSubscription:
      description: A Stripe Subscription. You need to annotate this yourself. An ID is included to make sure the generate creates a type from this, which we can overwrite.
      type: object
      properties:
        id: 
          type: string
