components:
  responses:
    CountriesResponse:
      description: A Countries collection response
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/CountriesCollection'
