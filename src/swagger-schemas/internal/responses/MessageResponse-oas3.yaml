components:
  responses:
    MessageResponse:
      description: A normal message response
      content:
        'application/json':
          schema:
            $ref: '#/components/schemas/Message'
