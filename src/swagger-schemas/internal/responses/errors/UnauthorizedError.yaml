components:
  responses:
    UnauthorizedError:
      description: Access token is missing or invalid
      content:
        'application/json':
          schema:
            type: object
            properties:
              status:
                type: number
                example: 401
              message: 
                type: string
